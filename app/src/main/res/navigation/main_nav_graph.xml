<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph.xml"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="fathian.ali.weatherapp.presentation.main.MainFragment"
        android:label="MainFragment"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_mainFragment_to_unitsDialog"
            app:destination="@id/unitsDialog" />
    </fragment>
    <dialog
        android:id="@+id/unitsDialog"
        android:name="fathian.ali.weatherapp.presentation.main.UnitsDialog"
        android:label="UnitsDialog"
        tools:layout="@layout/dialog_units"/>


</navigation>